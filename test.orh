start main
define as {
	print("Hello, World!");
} end end
// TODO: fix parser to automatically end function body when ending code block.
// RIght now we have end x2 because were ending the function block (start)
// and the code block (define as)
